# Rust
target/
**/*.rs.bk

# Node
node_modules/
dist/
.vite/

# Editors / OS
.DS_Store
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# Tauri's build output, which lives inside the shell crate
apps/desktop/src-tauri/target/

# The website's generated assets: the WebAssembly package wasm-pack writes,
# and the example stacks packed as single files. Both are built in CI.
apps/web/wasm-pkg/
apps/web/public/examples/
